name: Scheduled Maintenance

on:
  schedule:
    - cron: '10 10 * * *' # Every day at 10:00 UTC

jobs:
  backup:
    runs-on: ubuntu-latest
    steps:
      - name: Run backup script
        run: |
          mkdir -p backup
          cp -r src backup/

